import React, { Component } from "react"

const ProductDetails = props => {
  return (
    <div>
      <h1>Product Details : {JSON.stringify(props)}</h1>
    </div>
  )
}

export default ProductDetails
